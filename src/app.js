import fromSchema from './util/fromSchema'
import personSchema from './Person.json'

const Person = fromSchema(personSchema)
fromSchema(personSchema)
fromSchema(personSchema)
fromSchema(personSchema)

export default true
